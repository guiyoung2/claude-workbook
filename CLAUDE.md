@AGENTS.md

# 작업 워크플로우 규칙

- **패키지 매니저는 npm만 사용한다.** `pnpm`, `yarn`, `bun` 명령어를 절대 쓰지 말 것.
- **작업 시작 전 `plan.md`와 `fix.md`를 먼저 읽어** 프로젝트 흐름과 그동안의 결정·변경 이력을 파악할 것.
- **각 단계 완료 시 다음 세 가지를 모두 수행한다:**
  1. `plan.md`의 해당 단계 헤더에 `✅ 완료 (YYYY-MM-DD)` 마킹
  2. `fix.md`에 `- YYYY-MM-DD · 단계명 완료: 요약` 한 줄 추가
  3. 사용자에게 변경 파일·확인 방법 보고 후 다음 단계 진행 여부 확인
- **새 프로젝트 규칙이 생기면** 이 `CLAUDE.md`에 한 줄이라도 추가하여 재정의한다. 너무 길어지면 해당 영역 폴더(`app/CLAUDE.md`, `content/CLAUDE.md` 등)로 분리.
