---
title: 프로젝트 루트 실행과 /init
description: Claude Code를 어디서 실행해야 하는지, /init으로 CLAUDE.md를 자동 생성하는 방법을 알아봅니다.
date: 2026-05-08
order: 3
category: Claude
---

## 📁 어디서 실행하느냐가 생각보다 중요하다

Claude Code를 처음 설치하고 나면 "그냥 아무 데서나 실행하면 되지 않나?" 싶을 수 있습니다.
하지만 **어디서 실행하느냐**가 Claude의 파악 능력에 직결됩니다.

Claude Code는 실행한 디렉토리를 기준으로 파일을 탐색합니다.

```bash
# ❌ 잘못된 예 — 엉뚱한 폴더에서 실행
cd ~/Desktop
claude

# ✅ 올바른 예 — 프로젝트 루트에서 실행
cd ~/Desktop/my-project
claude
```

엉뚱한 폴더에서 실행하면 어떻게 될까요?

- Claude가 프로젝트 구조를 파악하지 못함
- 파일 경로를 잘못 찾거나 빈 디렉토리만 봄
- `src/components/Button.tsx`를 찾으라고 해도 못 찾음

**프로젝트 루트**는 `package.json`, `tsconfig.json`, `.git/`이 있는 바로 그 폴더입니다.

---

## 🛠️ /init — 프로젝트 설명서 자동 생성

처음 프로젝트에 진입했다면 `/init`을 먼저 실행하세요.

```
/init
```

Claude가 프로젝트를 분석해서 `CLAUDE.md` 파일을 자동으로 만들어줍니다.

- 📂 폴더 구조 파악
- ⚙️ 주요 설정 파일 분석 (`package.json`, `tsconfig.json` 등)
- 🔧 빌드·테스트 명령어 감지
- 📏 코드 패턴 파악

---

## 📋 CLAUDE.md란 무엇인가?

`CLAUDE.md`는 **매 대화마다 자동으로 주입되는 프로젝트 기억 장치**입니다.

LLM은 이전 대화를 기억하지 못합니다. 어제 정했던 코딩 컨벤션이나 폴더 구조를 다음 대화에선 전혀 모릅니다.

`CLAUDE.md`가 이 문제를 해결합니다. 대화가 시작될 때마다 컨텍스트 최상단에 자동으로 주입되어 Claude가 프로젝트 맥락을 매번 다시 인식합니다.

| 상태 | Claude의 행동 |
|------|--------------|
| `CLAUDE.md` **있음** | 프로젝트 컨벤션에 맞는 코드 생성, 올바른 폴더 구조 사용, 팀 규칙 준수 |
| `CLAUDE.md` **없음** | 일반적인 추측에 의존, 매번 같은 설명 반복, 프로젝트 맥락 부재 |

> `CLAUDE.md` = "Claude야, 우리 프로젝트는 이렇게 운영되고 있어"라고 매 대화 시작 시 자동으로 브리핑해주는 문서

---

## ✅ 새 프로젝트 시작 체크리스트

Claude Code를 처음 쓸 때 이 순서를 따르세요.

```
1. 프로젝트 루트로 이동   →   cd your-project
2. Claude Code 실행       →   claude
3. /init 실행             →   CLAUDE.md 자동 생성
4. 생성된 CLAUDE.md 검토  →   잘못된 내용은 수정 or Claude에게 수정 요청
```

CLAUDE.md를 잘 작성하는 방법은 다음 포스트에서 자세히 다룹니다.
